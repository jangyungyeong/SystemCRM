<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jafa.repository.product.PdCategoryRepository">
	
	<select id="readCategory" resultType="pdcategory">
		select * from pdCategory_tbl where parentCategoryId=#{parentCategoryId}
	</select>

</mapper>